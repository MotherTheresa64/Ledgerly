# Transaction input limits

Ledgerly constrains transaction text at both the browser and API layers:

- Description: 80 characters maximum
- Notes: 500 characters maximum

The API also applies these limits to CSV imports so client-side validation cannot be bypassed.
